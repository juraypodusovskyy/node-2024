export enum EOrder {
  ASC = "asc",
  DESC = "desc",
}

export enum EOrderBy {
  NAME = "name",
  CREATED_AD = "createdAt",
}

export enum EOrderByProduct {
  NAME = "name",
  PRICE = "price",
  CREATED_AD = "createdAt",
  WEIGHT = "weight",
  VIEWS = "views",
}
